# If you see this, run "rustup self update" to get rustup 1.23 or newer.
#
# Above comment is for older rustup (before TOML support was added), which will
# treat the first line as the toolchain name, and therefore show it to the user
# in the error

[toolchain]
channel = "nightly-2023-05-01"
targets = ["x86_64-pc-windows-gnu", "x86_64-pc-windows-msvc"]
components = ["rustfmt", "clippy", "miri", "rust-std", "rust-src", "rust-mingw"]
